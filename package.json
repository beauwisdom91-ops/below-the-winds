{
  "name": "below-the-winds",
  "private": true,
  "type": "module",
  "description": "Below the Winds scholarly used-book shop — catalog, direct checkout, Amazon till",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "npx netlify dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "netlify database migrations apply"
  },
  "dependencies": {
    "@netlify/database": "2.0.1",
    "@netlify/functions": "6.0.0",
    "drizzle-orm": "1.0.0-beta.22",
    "stripe": "22.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "drizzle-kit": "1.0.0-beta.22"
  },
  "engines": {
    "node": ">=20"
  }
}
