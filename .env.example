# Stripe (Dashboard → Developers → API keys). Use a restricted key (rk_live_ / rk_test_) if possible.
STRIPE_SECRET_KEY=
# Stripe webhook signing secret for /api/stripe-webhook
STRIPE_WEBHOOK_SECRET=
# Public site URL used in Checkout success/cancel redirects if context.site.url is missing
URL=https://below-the-winds.netlify.app
